# R005 雙職務擇優

## 使用者故事

身為薪資管理人員，
我希望系統在同月同時符合兩個互斥津貼資格時，
能自動擇優給付較高的津貼，
以避免重複給付。

## 驗收條件

- 當月同時符合兩個互斥津貼（例如 `allowanceA` 與 `allowanceB`）。
- 互斥組別由規則設定定義，僅能擇一。
- 給付金額為 `max(allowanceA, allowanceB)`。
- 互斥組別與津貼名稱來源為 `data/rules.json` 的互斥設定欄位。
