# R206 外交代表/聯合任務

## 使用者故事

身為薪資管理人員，
我希望系統能在任務屬於聯合任務時發放加給，
並可補助語言與禮賓支出，
以反映外交與協作成本。

## 驗收條件

- 給定任務是否為聯合任務 `joint_mission`。
- 若 `joint_mission = true`，則加給 `joint_mission_bonus`。
- 可額外補助語言或禮賓支出（與專業類規則連動）。
- `joint_mission_bonus` 由規則或參數設定提供。
- `joint_mission` 來源為 `data/missions.json` 的欄位。
