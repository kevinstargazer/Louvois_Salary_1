# R107 待命津貼

## 使用者故事

身為薪資管理人員，
我希望系統能針對待命時數計算津貼，
且在實際出勤時停止待命計算，
以避免重複給付。

## 驗收條件

- 給定待命時數 `standby_hours`。
- 待命津貼 = `standby_hours * standby_rate`。
- 若發生實際出勤（`called_in = true`），則待命津貼改以勤務規則計算。
- `standby_rate` 由規則或參數設定提供。
- `standby_hours` 與 `called_in` 來源為 `data/employees.json` 的欄位。
