# R402 公務交通票價實報

## 使用者故事

身為薪資管理人員，
我希望系統能在員工提供公務交通票據時，
依規定實報實銷並受上限限制，
以確保交通補貼正確發放。

## 驗收條件

- 給定票據金額 `receipt_amount` 與上限 `transit_cap`。
- 給付金額 = `min(receipt_amount, transit_cap)`。
- `transit_cap` 來源為 `data/lookup_table.json`。
- `receipt_amount` 來源由 `data/employees.json` 的欄位提供。
