# R001 基本薪資

## 使用者故事

身為薪資管理人員，
我希望系統能依「軍階/職等」查表計算基本薪資，
以確保每位人員的基本薪資正確且一致。

## 驗收條件

- 給定人員的 `rank`，系統可透過 `data/rules.json` 的 R001 設定，查到 `data/lookup_table.json` 的 `base_salary_table` 對應金額。
- 若找不到對應 `rank`，則回傳 0 或標示為資料異常。
- 計算結果為「基本薪資」，後續津貼/扣款可在此基礎上加減。
