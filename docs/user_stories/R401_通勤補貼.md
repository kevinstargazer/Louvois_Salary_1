# R401 通勤補貼

## 使用者故事

身為薪資管理人員，
我希望系統能依員工住址到駐地的距離，
按距離級距給付通勤補貼，
以確保通勤補貼正確發放。

## 驗收條件

- 給定通勤距離 `commute_km` 與級距表 `commute_allowance_band`。
- 依 `commute_km` 落點級距給付對應補貼金額。
- `commute_allowance_band` 來源為 `data/lookup_table.json`。
- `commute_km` 來源由 `data/employees.json` 的欄位提供。
