# R203 戰區危險加給

## 使用者故事

身為薪資管理人員，
我希望系統能在派遣地點屬於戰區時發放危險加給，
以反映戰區勤務的風險。

## 驗收條件

- 給定派遣地點是否屬戰區 `hazard_zone` 與派遣天數 `trip_days`。
- 危險加給可用兩種方式擇一：
  - 每日本薪 * `hazard_rate`
  - 固定額 `hazard_flat`（按天）
- `hazard_rate` 與 `hazard_flat` 由規則或參數設定提供。
- `hazard_zone` 與 `trip_days` 來源為 `data/missions.json` 的欄位。
