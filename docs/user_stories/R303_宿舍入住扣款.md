# R303 宿舍入住扣款

## 使用者故事

身為薪資管理人員，n我希望系統能在使用軍方宿舍時，
依規定扣除宿舍費用，
以確保住宿扣款正確計入薪資。

## 驗收條件

- 給定是否使用宿舍 `barracks_provided` 與扣款金額 `barracks_fee`。
- 若使用宿舍，則扣除 `barracks_fee`（可視規則為固定或按天計）。
- `barracks_fee` 來源為 `data/lookup_table.json`。
- `barracks_provided` 來源為 `data/units.json` 的欄位。
