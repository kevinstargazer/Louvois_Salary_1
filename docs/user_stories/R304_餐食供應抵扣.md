# R304 餐食供應抵扣

## 使用者故事

身為薪資管理人員，
我希望系統能在軍方提供餐食時，
依規定減少日支或相關補貼，
以確保餐食供應抵扣正確計入薪資。

## 驗收條件

- 給定是否提供餐食 `meals_provided` 與扣抵金額 `meal_deduction`。
- 若提供餐食，則扣抵 `meal_deduction`（可視規則為固定或按餐別拆分）。
- `meal_deduction` 來源為 `data/lookup_table.json`。
- `meals_provided` 來源由 `data/units.json` 的欄位提供。
