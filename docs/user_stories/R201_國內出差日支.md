# R201 國內出差日支

## 使用者故事

身為薪資管理人員，
我希望系統能依國內出差地區級距計算日支，
以確保差旅補貼正確發放。

## 驗收條件

- 給定出差地區級距 `domestic_zone` 與出差天數 `trip_days`。
- 國內出差日支 = `trip_days * per_diem_domestic[domestic_zone]`。
- `per_diem_domestic` 由規則或參數設定提供。
- `domestic_zone` 與 `trip_days` 來源為 `data/missions.json` 的欄位。
